{
  "name": "filanccia",
  "version": "0.1.0",
  "description": "Filanccia - A mystery adventure game",
  "main": "src/main.js",
  "scripts": {
    "start": "http-server . -p 8080 -c-1",
    "build": "shx rm -rf dist && shx mkdir -p dist && shx cp index.html dist/ && shx cp -r src dist/ && shx cp -r assets dist/",
    "build:win": "if not exist dist mkdir dist && copy index.html dist\\ && xcopy /E /I /Y src dist\\src && xcopy /E /I /Y assets dist\\assets"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "phaser": "^3.55.2"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "shx": "^0.3.4"
  }
}
